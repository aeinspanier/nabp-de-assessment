name: 'de_assessment'
version: '1.0.0'

profile: 'de_assessment'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"


models:
  de_assessment:
    +materialized: view
    staging:
      +schema: staging
    intermediate:
      +schema: intermediate
